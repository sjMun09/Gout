server:
  # 테스트 프로필은 XFF 해석을 비활성. MockMvc 기본 remoteAddr 가 고정이라
  # XFF 를 변조해도 RateLimitFilter 의 bucket 이 분리되지 않음을 직접 검증 가능 (P1-11 회귀 테스트).
  forward-headers-strategy: NONE

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    # V25 가 CONCURRENTLY 를 쓰므로 트랜잭션 밖 실행 허용.
    mixed: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 25MB

app:
  uploads:
    base-dir: ${java.io.tmpdir}/gout-test-uploads
  cors:
    # 테스트 프로파일 CORS 화이트리스트. CorsConfigIntegrationTest 가 이 값에 의존.
    allowed-origins: http://localhost:3000,http://localhost:3010

jwt:
  secret: test-secret-key-must-be-at-least-256-bits-long-for-hs256-xxxxx
  access-token-expiry: 900000
  refresh-token-expiry: 604800000

logging:
  level:
    com.gout: INFO
    org.hibernate.SQL: WARN
    org.testcontainers: INFO
